import React from 'react';
import {Text, View} from 'react-native';

const AppUnAuth = () => {
  return (
    <View>
      <Text>This is reusable login and register screen ui</Text>
    </View>
  );
};
export default AppUnAuth;
